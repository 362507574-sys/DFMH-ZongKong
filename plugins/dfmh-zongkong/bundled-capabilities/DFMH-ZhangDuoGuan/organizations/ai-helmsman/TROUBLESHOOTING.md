# AI掌舵官故障处理

## 知识检索无命中

保存`no_hit`凭证并继续企业分析，不得声称使用了知识库内容。

## 知识检索降级

保存`degradedReason`并继续；只有临时网络或服务错误可有限重试。

## 企业或任务不匹配

立即停止，不自动移动、复制或合并跨企业资料。

## 候选哈希不一致

拒绝晋级，保留原批准记录；建立新候选版本并重新审核。

## 同一问题重复失败

同一根因第三次失败进入`failed`终态，保留错误码和证据，不再自动复活。
