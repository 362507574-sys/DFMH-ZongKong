# 品AI品牌官变更记录

## 2026-07-30

### 三个核心 Skill v2 完整运行链

- 为品牌定位、品牌视觉、品牌传播接入共用的任务规划、证据引擎、质量门禁、调试控制和双层成果打包；五个组件均为运行支撑，不新增第四个 Skill。
- 建立项目内隔离工作区、严格 JSON Schema、稳定哈希、精确 `artifactId@version + SHA-256` 依赖、双审核、同根因三轮停止、任务租约、幂等恢复和交付承诺链。
- 定位 Skill 完成品类、用户、差异化、心智占位四模块的确定性路由、证据要求、双审核与双层候选交接。
- 视觉 Skill 完成视觉体系、门店、海报、包装、AI视觉生成五模块；增加专用语义校验、恰好三个方向与两两差异、项目隔离审美档案，以及普通海报公共 Skill 的动态三重授权和品牌质量否决。
- 传播 Skill 完成内容传播、品牌战役、品牌故事、创始人 IP 四模块；增加专用语义校验、定位/视觉依赖矩阵、增长与成交越界拆分、确认主张三方摘要绑定，以及不可变传播策略上下文与迁移审计。
- 建立定位 → 视觉 → 传播的真实运行时端到端夹具，覆盖同项目精确版本交接，以及上游哈希漂移、跨项目读取、缺少定位/视觉、审美档案跨项目、公共海报未授权和三轮硬否决等失败链。
- 同步统一 Workflow、用户使用指南、环境入口、故障恢复和长期决策；当前仍为总控调用的组织分支，只有三个核心 Skill，根权威保持 `designing`、`acceptsFormalTasks = false`，组织本地为 `registered_designing`。
- 实际验证历史：原三个 Skill 与集成回归 20/20；全部组织 Node 测试首次 372/372；随后一次全量并行复跑为 370/372，恢复锁活跃守卫子测试失败并连带父级计为失败，隔离子测试 1/1、当时完整运行时文件 56/56。该 370/372 继续保留为发现问题的真实证据。
- 恢复守卫并发问题严格按 TDD 关闭：根因是守卫在 `lstat` 成功后、`realpath` 前被合法释放，导致 `realpath` 返回 `ENOENT`；新增确定性 RED 后，只在恢复守卫回收路径把该 `ENOENT` 识别为“已经合法释放”，不放宽其他错误或符号链接防护。目标父组 5/5、完整运行时 57/57、独立代理 fresh 全量 373/373。
- 状态复核：本地登记为 `registered_designing`，三个 Skill 均为组织侧 `pilot`；根权威仍为 `designing`、`acceptsFormalTasks = false`。恢复守卫并发问题已关闭，但真实企业任务、用户现场审美和真实跨组织回传尚未执行，不宣称正式运营或真实业务验收完成。
- 完整验证凭证：`temp/skill-evals/core-skills-v2-verification.md`。

### 独立审查修正

- 修正 `skills/README.md` 的证据等级：三个 Skill 已完成的是自动化候选验证，真实企业任务、用户现场验收和真实跨组织回传仍待执行。
- 将组织本地登记与根正式运营就绪度分开验证：集成测试直接读取 `config/organization.json` 断言 `registered_designing`，并独立读取根权威断言 `designing`、`acceptsFormalTasks = false`；删除把正式就绪度派生成 `rootControllerRegistration = not_connected` 的旧语义。
- TDD 记录：保留旧派生逻辑时新增语义回归为 RED（8 项中 7 项通过、1 项失败）；删除旧逻辑后集成测试 GREEN 为 7/7。
- `WORKFLOWS.md` 将普通宣传海报当前根权威统一为 `operational`；`formal` 只保留为历史组织侧分类说明，运行时以根公共登记表为准。
- 调整长期决策展示顺序为 D-009 在 D-010 之前，不改变任何决策内容与效力。
- 本轮相关 Skill、文档、质量和集成定向回归为 62/62，通过。

## 2026-07-29

### 统一“快、准、稳”质量整改

- 为品牌定位、品牌视觉、品牌传播分别建立独立完整试运行 Workflow，补齐适用、输入门禁、执行、输出验收、异常重试、停止条件与真实企业验收边界。
- 新增 `quality/organization-quality.json`，用统一结构登记三个 Skill 的路径、运行依赖、测试证据、证据等级、已知缺口，以及组织级快、准、稳能力。
- 根运行登记已经能定位 AI品牌官配置和三个 Skill，因此组织本地 `rootControllerRegistration` 从历史 `not_connected` 对齐为 `registered_designing`；根权威继续保持 `designing`、`acceptsFormalTasks = false`。
- 未修改根控制中心、根注册表、根运行映射、根测试或 root-owned `ORGANIZATION.md`，不宣称正式上线。

## 2026-07-28

### Task2｜组织骨架与真实能力配置

- 建立组织身份配置与品牌官能力配置，固定三个组织核心技能的组织侧候选配置；根权威成熟度始终以控制中心注册表为准。
- 分离两个根级公共能力引用，普通宣传海报保持 `formal`，淘宝电商套图保持 `pilot`，均不计入核心技能。
- 固定企业档案、定位临时任务和预留输出路径；当前不得向预留输出路径写入正式成果。
- 飞书知识写回保持 `disabled`；当时的根控制中心本地登记为历史 `not_connected`，同级组织调用保持 `contract_only`。当前登记已由 D-011 更新为 `registered_designing`，本条不表示当前状态。
- 增加两份 JSON Schema 2020-12 严格约束，拒绝未知字段、路径漂移、能力混入和虚假连接状态。
- 增加 13 项配置契约测试，并在系统临时副本中验证改写或删除重建 root-owned 章程均会失败。
- 建立 Workflow、程序公共库、企业档案、定位临时任务、正式交付、问题和 Skill 目录说明。
- TDD 记录：配置与 Schema 缺失时测试为 RED（2 项通过、11 项失败）；补齐最小实现后为 GREEN（13 项通过、0 项失败）。
- 本任务未修改 `ORGANIZATION.md`、根注册表、根路由或根公共技能资产，也未宣称根级连接已经完成。

### Task3｜组织规则、流程与非技术说明

- 新增组织级 `AGENTS.md`，继承根规则并固化读取顺序、组织目录边界、四阶段正式流程、知识前置和结果委托。
- 新增 `ORGANIZATION_DETAILS.md` 承载架构、三个核心技能、企业隔离、候选与正式隔离、主责不转移和真实连接边界；root-owned `ORGANIZATION.md` 保持原始字节完全不变。
- 新增统一 `WORKFLOWS.md`，固化“控制中心或组织直达 → 企业品牌绑定 → 飞书知识 → 对话补充 → 互联网公开检索 → 证据分类 → 定位试运行 → 协作候选与审核 → 候选定位 → 品牌总审 → 用户反馈 → 候选与验证证据归档”链路。
- 新增非技术 `USER_GUIDE.md`、长期 `DECISIONS.md`、实际验证 `ENVIRONMENT.md`、故障处理 `TROUBLESHOOTING.md` 与组织问题导航。
- 明确普通宣传海报 = `formal` 公共能力，淘宝电商套图 = `pilot` 公共能力；不把公共能力计入品牌官三个核心技能。
- 文档当时保持历史 `rootControllerRegistration = not_connected`、`peerOrganizationCalls = contract_only`，不扩大根路由、根调度、同级组织或正式接单权限。当前本地登记已由 D-011 更新为 `registered_designing`，根权威仍非正式运营。
- 引用根正式审查凭证 `temp/control-center/protected-root-baseline-reviews/20260728-ai-brand-officer-task1/review-result.json`：旧快照 `76519e2e…0f2bb` 已完整备份，新快照为 `d37f9736…7ec3`；凭证记录品牌官 17/17、根级 816/816、自检 72 文件/10 目录/0 问题、最终范围守卫退出码 0。
- 上述根审查只解除明确的范围门禁，不代表根注册连接、正式接单或其他根权限；组织侧 rebaseline 仍永久拒绝。
- TDD RED：文档实现前 34 项中 21 项通过、13 项因缺少 Task3 文档失败；所有章程退化变异仅在系统临时副本中执行。
- Task3 GREEN：文档契约 34/34；Task1 + Task2 + Task3 联合回归 65/65。
- 历史根自检兼容缺口已由根级 `ISSUE-0040` 修复并关闭；正式凭证记录当前自检 72 文件/10 目录/0 问题，并拒绝 53 类伪装导航。组织导航只引用根级解决项，不复述正文。

### Task3质量修复｜根权威成熟度与导航收口

- 直接绑定根注册表真实状态：AI品牌官 `status = designing`、`acceptsFormalTasks = false`，三个核心技能均为 `designing`。
- 明确组织配置的品牌定位 `pilot` 仅是组织侧候选验证成熟度，不等于根权威成熟度。
- 在根升级为 `operational` 且 `acceptsFormalTasks = true` 前，品牌官只形成候选与验证证据；用户验收只形成候选反馈与试运行证据，不触发正式接单、正式晋级或 `outputs/` 正式写入。
- 增加逐文件成熟度门槛测试、根注册表直接读取测试和等价虚假声明退化测试；保留全部 root-owned 章程退化测试。
- 组织问题导航删除临时问题正文，只引用根级第 0040 项（已解决）与根级唯一事实源。
- 质量修复 TDD：新增契约后 RED 为 38 项中 34 项通过、4 项失败；修正文档与导航后 GREEN 为 38/38。

### Task4｜三个核心 Skill 首轮建设

- 建立 `brand-positioning`、`brand-visual`、`brand-communication` 三个完整 Skill；每项均包含适用场景、输入、步骤、输出、依赖、质量检查、异常处理、重试、停止条件、示例、版本、交付契约、候选模板与调用说明。
- 品牌视觉自动继承已确认的品牌定位；品牌传播自动继承已确认的品牌定位与视觉规范。
- 品牌传播固化品牌与增长边界：负责品牌信息、内容母题、真实品牌故事、创始人 IP、重大品牌传播战役和一致性审核；不接管抖音、小红书、公众号、私域的日常运营、投放获客、客户培育、复购设计或成交话术。
- 独立审查后进一步拆清协作职责：AI增长战略官承接日常平台运营、投放获客、客户培育、转化与复购；AI成交官承接销售沟通、成交话术、成交脚本与成交策略；传播 Skill 分别输出两份协作简报。
- 三项 Skill 均完成无 Skill 基线与带 Skill 独立评测，组织配置统一升级为 `pilot`；根注册表仍保持 `designing` 与 `acceptsFormalTasks = false`。
- 增加三个单项 Skill 契约测试和三 Skill 集成测试；先观察到缺文件或成熟度不一致的 RED，再完成 GREEN。
- 未修改 root-owned `ORGANIZATION.md`、根注册表、根路由或公共技能，也未向 `outputs/` 写入正式成果。

### 架构表述修正｜总控调用的组织分支

- 明确 AI品牌官不是总控主线、控制中心或总体调度器，只是由总控按任务调用的业务组织分支。
- 明确该分支内部只拥有品牌定位、品牌视觉、品牌传播三个核心 Skill。
- 删除“组织直达入口”表述；统一为总控识别任务、分派并调用 AI品牌官分支。
- “品牌任务主责”仅表示分派后对该品牌任务的结论与总审负责，不授予根路由、其他组织调度或系统主线权限。
