# 项目隔离舱协作边界

AI品牌官只写当前业务项目 `organizations/ai-brand-officer/` 工作区。

品牌定位、品牌视觉和品牌传播需要使用其他组织成果时，只读取控制中心写入任务上下文的精确版本和SHA-256；不得扫描其他组织目录、读取其他客户项目或使用模糊的“最新文件”。

候选成果通过组织门禁后，由控制中心发布到当前项目 `shared-artifacts/`。AI品牌官不得直接写共享成果区、切换当前版本或覆盖历史版本。此规则不改变受保护的 `ORGANIZATION.md` 字节。
