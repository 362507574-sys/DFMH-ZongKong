# 项目隔离舱协作边界

AI成交官只写当前业务项目 `organizations/ai-deal-officer/` 工作区。

跨组织输入必须来自当前任务 `project-context.json` 列出的精确共享成果版本和SHA-256；不得扫描其他组织目录、读取其他项目或使用模糊的“最新文件”。

候选成果通过组织门禁后，由控制中心发布到当前项目 `shared-artifacts/`。AI成交官不得直接写共享成果区，不得覆盖历史版本，也不得自行建立跨项目引用。
