# 快速开始｜DFMH总控

## 1. 安装前准备

- 使用能够访问 GitHub 的 Codex。
- 完整安装包含5个组织、15个组织技能、2个公共技能。
- 飞书是可选能力；不配置飞书也能使用本地控制中心。
- 公开包不包含发布者的账号、密钥、客户资料或历史任务。

## 2. 推荐方式｜让Codex全自动安装

在新的 Codex 对话中发送：

> 请安装并初始化这个Codex插件：https://github.com/362507574-sys/DFMH-ZongKong 。采用完整安装，自动完成依赖检查和自检；成功后列出5个组织、15个组织技能、2个公共技能及各自成熟度。

Codex 应自动完成：

1. 添加 DFMH 市场源。
2. 安装 `dfmh-zongkong@dfmh`。
3. 调用 `dfmh-zongkong-bootstrap`。
4. 创建本机运行目录并解压7个固定版本能力包。
5. 运行完整性、隐私、依赖和初始化自检。

## 3. 手动命令备用方式

只有自动安装未能调用插件命令时，才让 Codex 在终端依次执行；使用者不需要自己理解参数：

```powershell
codex plugin marketplace add 362507574-sys/DFMH-ZongKong
codex plugin add dfmh-zongkong@dfmh
```

然后新开对话并发送：

> 初始化DFMH总控。请执行初始化脚本并返回真实状态，不要只回复“已收到”。

## 4. 初始化成功的验收标准

必须同时满足：

- 初始化脚本真实返回 `status=installed`。
- 能力包数量为7：五个组织包＋海报设计＋淘宝套图。
- 总控能列出5个组织、15个组织技能、2个公共技能。
- 每个组织同时显示自身成熟度和正式任务许可；安装不能把 `designing` 伪装成正式可用。
- 飞书未配置时显示 `feishu=not_configured`，而不是安装失败。

仓库根目录执行 `npm test` 验证的是公开源码包和干净安装模拟；它不能单独代替上面的本机安装状态回执。

## 5. 下达第一个项目

安装验收通过后发送：

> 项目：成都九眼桥24小时健身房可行性调查。目标是判断商圈、竞争、投资预算和差异化方向。请先判断主责组织与必要技能，只调用真正需要的能力；缺少会改变结论的信息时一次性最少追问，然后完成第一版成果。

总控应先返回项目编号、主责组织和选用技能，再开始执行。之后的“修改、补充、重新汇报”都应留在同一项目，不得另建项目。

## 6. 普通问答与单组织使用

- 普通问答直接提问，不要加“项目：”，总控不应创建项目。
- 只需要某一个组织时，可以下载对应组织仓库，把文件夹作为独立 Codex 项目打开；具体步骤见该仓库 `QUICKSTART.md`。
- 不要手工把五个组织复制到同一个目录，总控已经按固定版本打包。

## 7. 更新到最新版本

在 Codex 中发送：

> 请从 https://github.com/362507574-sys/DFMH-ZongKong 更新到最新 main 版本，保留我本机的配置和项目资料，刷新插件后重新运行初始化与自检，并汇报新的插件版本和能力包版本。

如果 Codex 的插件页面显示“刷新（Refresh）”，也可以选择刷新后再发送上面的初始化与自检要求。

## 8. 飞书配置

只填写安装者自己的飞书应用信息。没有飞书配置时，本地控制中心仍可运行；不要把密钥提交到 GitHub，也不要使用发布者的凭据。
